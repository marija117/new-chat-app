<h1>
  <%= @room.name %>
</h1>

<div class="row">
  <div class="col-12 col-md-3">
    <%= render partial: 'rooms' %>
  </div>

  <div class="col">
    <div>
      <app :room_id="<%= @room.id %>" :current_user="<%= current_user.id %>" id='room'></app>
    </div>
  </div>
</div>