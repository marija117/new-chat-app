<h1>
  <%= @room.name %>
</h1>

<div class="row">
  <div class="col-12 col-md-3">
    <%= render partial: 'rooms' %>
  </div>

  <div class="col">
    <div id="chat">
      <chat-room :room_id="<%= @room.id %>" :current_user="<%= current_user.id %>" id='room'></chat-room>
    </div>
  </div>
</div>